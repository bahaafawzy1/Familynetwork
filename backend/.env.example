# Backend environment variables
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/fcnetwork?schema=public

# Server
PORT=4000
LOG_LEVEL=info
CORS_ORIGIN=http://localhost:5173

# JWT
JWT_SECRET=change-me
JWT_TTL=7d

# SMTP (optional for sending OTP via email)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
MAIL_FROM=no-reply@example.com

# Twilio (optional for SMS/WhatsApp OTP)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_SMS_FROM=
TWILIO_WHATSAPP_FROM=